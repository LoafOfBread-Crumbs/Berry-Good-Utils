<Window x:Class="BerryGoodUtils.Modules.QuoteGenerator.CompanySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Company Settings" Height="420" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResizeWithGrip"
        MinWidth="420" MinHeight="360"
        Background="#f0f4f8">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Padding" Value="6,5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#cbd5e1"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Company Information" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource BerryBlueBrush}" Margin="0,0,0,16"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Company Name" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtCompanyName" Style="{StaticResource InputBox}"/>

                <TextBlock Text="Address" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtCompanyAddress" Style="{StaticResource InputBox}"/>

                <TextBlock Text="Phone" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtCompanyPhone" Style="{StaticResource InputBox}"/>

                <TextBlock Text="Email" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="txtCompanyEmail" Style="{StaticResource InputBox}"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Padding="16,8" Margin="0,0,8,0" Click="Cancel_Click"
                    Background="#e2e8f0" Foreground="#334155" BorderThickness="0" FontSize="13" Cursor="Hand"/>
            <Button Content="Save" Padding="16,8" Click="Save_Click"
                    Background="{StaticResource BerryBlueBrush}" Foreground="White" BorderThickness="0" FontSize="13" FontWeight="SemiBold" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
